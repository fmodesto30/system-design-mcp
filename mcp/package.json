{
  "name": "system-design-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server (stdio) exposing the System Design Specialist Lab knowledge base as tools. No LLM at runtime.",
  "bin": {
    "system-design-mcp": "dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "smoke": "node test/smoke.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
